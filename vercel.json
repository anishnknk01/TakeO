{
  "buildCommand": "npm run build",
  "framework": "nextjs", 
  "installCommand": "npm install && npx prisma generate",
  "ignoreCommand": "git diff --quiet HEAD^ HEAD -- ':(exclude)prisma/'",
  "functions": {
    "pages/api/**/*.js": {
      "maxDuration": 30
    }
  }
}
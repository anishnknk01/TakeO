{
  "buildCommand": "npm run build",
  "framework": "nextjs",
  "installCommand": "npm install",
  "ignoreCommand": "git diff --quiet HEAD^ HEAD -- ':(exclude)prisma/seed.ts' ':(exclude)prisma/seed.ts.backup'",
  "functions": {
    "pages/api/**/*.js": {
      "maxDuration": 30
    }
  }
}
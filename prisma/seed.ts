// Empty seed file to prevent deployment errors
// This file is intentionally minimal to avoid any import issues

console.log('Seed file loaded (no operations)');

// No exports, no imports, no dependencies
export {};